export type ArtistsTypes = {
  id: string;
  name: string;
  playlistId: string;
  thumbnail?: string;
  thumbnailM?: string;
  totalFollow?: string;
  cover?: string;
  link?: string;
  alias?: string;
};
